<%# 
  eventDetail.ejs
  Shows details for a single event, including navigation to next/previous events.
%>

<%- include('../Partials/header') %>

<section class="mb-4">
  <h2 class="mb-3"><%= event.title %></h2>
  <img src="<%= event.image %>" alt="<%= event.title %>" class="img-fluid mb-3">
  <p><strong>Date:</strong> <%= event.date %></p>
  <p><strong>Location:</strong> <%= event.location %></p>
  <p>Lorem ipsum dolor sit amet, consectetur adipiscing elit. This is a placeholder for the event description.</p>

  <div class="d-flex justify-content-between mt-4">
    <% if (prevIndex !== null) { %>
      <a class="btn btn-outline-secondary" href="/events/<%= prevIndex %>">← Previous</a>
    <% } else { %>
      <span></span>
    <% } %>

    <% if (nextIndex !== null) { %>
      <a class="btn btn-outline-primary" href="/events/<%= nextIndex %>">Next →</a>
    <% } %>
  </div>
</section>

<%- include('../Partials/footer') %>
